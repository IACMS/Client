# IACMS API gateway (see IACMS/services/api-gateway). No trailing slash.
VITE_API_URL=http://localhost:3000

# LAN / network dev (other devices cannot use localhost — use your machine's IP):
# VITE_API_URL=http://192.168.1.42:3000
# Then: npm run dev -- --host
